const InitialState = () => {
    return {
        app: {
            auth: {},
            selectedId: null,
            items: [],
            formSubmitted: false
        }
    };
};

export default InitialState;
